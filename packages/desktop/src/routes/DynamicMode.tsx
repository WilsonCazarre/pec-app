import React from "react";
import Index from "../components/DynamicController";

const DynamicMode: React.FC = () => {
  return (
    <div>
      <Index />
    </div>
  );
};

export default DynamicMode;
